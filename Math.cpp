#include <iostream>
#include <cmath>

int main(int argc, char const *argv[])
{
    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = pow(2, y);
    
    std::cout << z;

    return 0;
}
